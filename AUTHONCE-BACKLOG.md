# AuthOnce — Working Backlog

**How this file works, and why it exists:** CLAUDE-CORE.md is the historical record — it explains *why* things happened, session by session. This file is different on purpose: it's a flat, numbered list of *open* items only, nothing narrative. Work only comes from this list. An item doesn't leave this list until it's been verified closed with real evidence (on-chain read, live deploy check, actual test run) — not just "looks done."

**Session start ritual, going forward:**
1. Upload this file + `CLAUDE-CORE.md` at the start of every session.
2. Pick ONE item. Work it to done or to a clearly logged blocker.
3. If a new problem surfaces mid-task that isn't already on this list, STOP, add it here with a status, and explicitly decide: fix now, or log and return to the original item. Don't silently tunnel deeper.
4. Update this file's status column before ending the session. If a fix was made but not deployed/tested, it is NOT done — mark it "built, unverified."

**Status legend:** `OPEN` (not started) · `IN PROGRESS` · `BUILT, UNVERIFIED` (code written, not deployed/tested live) · `BLOCKED` (external dependency) · `DONE` (verified with evidence)

---

## PART 1 — Business / CEO (most important first)

| # | Item | Status | Next concrete action |
|---|------|--------|----------------------|
| B1 | Solo founder, full-time job, no team absorbing detours | OPEN, structural | Not "fixable" directly — this backlog + pre-flight discipline exists specifically to reduce the cost of this constraint |
| B2 | No co-founder | OPEN | Continue search; explicitly half the funding-ask use-of-funds |
| B3 | AuthOnce Lda. incorporation not done | OPEN | Empresa Online (~€180) — blocks most grant eligibility, do before chasing more grants |
| B4 | Hashlock audit-scope email still unsent | OPEN — longest-idle item across many sessions. No budget for audit currently. Payment terms confirmed (2026-08-22): full amount upfront, no deposit/milestone option, not open to negotiation. | Send it anyway — you still need the quote itself to know the number, even though how it's paid is settled. Draft exists / can be redone in 10 minutes. |
| B4a | Mainnet is a hard financial gate, not a schedule item | OPEN, confirmed 2026-08-22 — audit requires full payment upfront before work starts. No funds, no audit, no mainnet — not a risk, a fact. B6 funding is externally blocked and unpaced (no reply from Base Fund; Circle needs mainnet traction; IAPMEI pending). | No fixed mainnet target date carried in this backlog as of 2026-08-22, per Vasco's direction — the Q3 2026 target is removed here. (Note: the Project's own description/custom instructions still state "Q3 2026" and this session has no way to edit those — only Vasco can change them in claude.ai project settings.) Do not reintroduce a specific launch date in this file until B6 funding actually lands and B4's quote is in hand. |
| B5 | Safe multisig is only 2-of-2, no third signer | OPEN | Tonight (Aug 18) proved directly how fragile this is — one signer unreachable stalls everything. Recruit/designate a third signer. |
| B6 | No funding secured yet | BLOCKED (external) | Base Ecosystem Fund resubmitted, no reply. Circle Alliance needs mainnet traction. IAPMEI/Startup Portugal pending. Follow up on Base Fund if no reply by early Sept. |
| B7 | Public content overclaiming vs. actual architecture, repeated pattern | OPEN | `/docs` describes Stripe-shaped fiat flow + universal gasless permits (false — USDT has no permit support) + live-sounding paid tiers (tier→access not built). Needs a full content-accuracy pass, not a patch. |
| B8 | Zero organic discovery — authonce.io not indexed by Google at all | OPEN | `site:authonce.io` returns nothing. Check Search Console verification + crawl status + robots.txt/sitemap (needs manual check, not verifiable by Claude's tools) |
| B9 | Merchant Portal (`MerchantDashboard.jsx`) has zero mobile/responsive support | OPEN, scoped 2026-08-22 via screenshots + code review. Root cause confirmed: no `@media` queries, no width-based layout switching anywhere in this 168KB component — every layout is a fixed desktop grid (`repeat(4,1fr)` stat cards, 5-7 column tables). Three concrete symptoms seen live on mobile: (1) top nav overflows horizontally and clips ("Base"/"My"/"Merchant"/"Wrong" cut off), (2) a raw error banner is permanently visible on the portal — "Chain not configured" + library version numbers exposed to end users, next to a "Wrong network" state, suggesting a real wagmi chain-config bug on mobile wallet browsers, not just styling, (3) the "How Pricing Works" info card (line ~2360) renders one word per line for ~30 lines — a nested-grid shrink bug, exact ancestor not pinned down without live DOM inspection. | Real scope, not a quick patch: needs breakpoints across nav, all 4 stat-grids, both data tables, and this card, plus separately diagnosing the chain-switch error (that one might be a functional bug affecting real mobile wallet users, worth prioritizing over pure layout). Vasco to decide: full responsive pass now, or patch the chain-config error first (most consequential) and schedule the layout work. |

---

## PART 2 — Technical (most important first)

| # | Item | Status | Next concrete action |
|---|------|--------|----------------------|
| T1 | `keeper.js` polls every active subscription every 20s regardless of due date | IN PROGRESS — pre-filter deployed, confirmed live via Aug 21 boot log (`"Due window: 4h (active subscriptions pre-filtered)"`; DB scan line showed only due-window subs, not the full active count) | Monitor Alchemy `eth_call` CU volume over the next several days to confirm it actually drops from the Aug 9–17 baseline before marking DONE. One clean boot is not sufficient evidence on its own. |
| T2 | `keeper.js` scheduler doesn't wait for previous cycle to finish (`setInterval`, not self-rescheduling) | IN PROGRESS, strong evidence — now 4 independent sampling windows, all healthy, spanning ~24.5h: Aug 21 18:12 (1 cycle), Aug 22 06:17–06:21 (11 cycles), Aug 22 18:22–18:30 (many cycles), Aug 22 18:44–18:47 (10 cycles). All show ~20–21s cadence, cycle times 667–756ms, no overlap, no repeated boot banner. Crash-loop question resolved: NOT crash-looping — separate-service boot banners (`AuthOnce — Merchant & Admin API` at 18:22:22, `x-bot` at Aug 21 17:14:36) confirmed distinct from the keeper's own cycles, appearing in the same combined paste. | Still not DONE: these are 4 discontinuous snapshots, not continuous multi-day logging, and Alchemy CU dashboard trend (the real proof of T1's cost benefit) still not observed. Keep sampling; no further crash/stacking concern. |
| T3 | `notifier.js` proactive-notification loop is fully sequential, no batching | OPEN | Lower urgency than T1/T2. Revisit after T1 lands and real load-test numbers exist. |
| T4 | 100-transaction volume/load test still never completed | OPEN — blocked by a chain of smaller issues across multiple sessions | Do NOT resume until T1/T2 are confirmed DONE (not just deployed) — testing on top of an unconfirmed cost/reliability fix wastes the test. Sequence: T1/T2 confirmed → resume test. RPC provider (T5) is now decided, no longer a blocker. |
| T5 | No stable, decided RPC provider strategy | DONE — Alchemy Pay-As-You-Go upgrade confirmed active via billing screenshot Aug 21 2026 (monthly, auto-renews Sep 1 2026, $0.45/M CUs 0–300M / $0.40/M over, 10,000 CU/s — matches this item's own Aug 20 cost estimate exactly). Aug 21 keeper boot log also confirms the RPC URL is actually pointed at Alchemy, not the public endpoint. | None — decision made and confirmed live. |
| T6 | Recurring pattern: duplicate/conflicting "sources of truth" in the codebase | OPEN — found 3 instances without even looking systematically (dead `wagmiConfig`, DB-only merchant status vs. on-chain, MerchantRegistry vs. SubscriptionVault having different admins) | Worth one dedicated session doing a systematic hunt across the whole codebase, not just fixing them as they're tripped over |
| T7 | CDP faucet API broken — confirmed via Coinbase's own CLI, not a local bug | BLOCKED (external) | Needs an actual Coinbase CDP support ticket with the reproducible error + trace ID. Manual portal faucet works as a workaround meanwhile. |
| T8 | SaaS tier billing success doesn't grant dashboard tier access | OPEN — confirmed gap, not built | Real problem the moment anyone actually pays for Growth/Business. Needs design + build. |
| T9 | Misleading RPC error messages mask real reverts (recurring pattern, not one bug) | OPEN | Happened repeatedly: Safe permission checks, wrong Basescan field. Also open as a `PayPage.jsx` pre-flight-check item specifically. Worth a consistent pattern/fix across the app, not per-instance patches. |
| T10 | Merchant-list live on-chain status badge fix | BUILT, UNVERIFIED — status corrected 2026-08-22, prior "local unpushed diff" claim was stale/wrong. Verified via git on Vasco's machine: `AdminDashboard.jsx` has zero uncommitted diff; commit `aa8430e` ("remove DB-only fake approve/revoke buttons, replace with real on-chain approve/revoke") is already pushed to `origin/main` (Aug 16, confirmed via reflog `update by push`). Local `main` = `origin/main` at `087e3ec`, same commit backing T1/T2. | Code is pushed, not "unpushed." Real remaining gap: confirm it's actually DEPLOYED (repo has both `netlify.toml` and `frontend/railway.json` — unclear which is live, and whether it auto-deploys on push) and verify the on-chain badge against a real merchant's `isApproved()` read on Basescan. Check the frontend hosting service's deploy history for `aa8430e` or later. |
| T11 | WooCommerce plugin cart/checkout annotation bug | OPEN — status unconfirmed recently | Check Console for JS error + inspect Store API response for `extensions.authonce` presence |
| T12 | `keeper_pull_attempts` — merchant-revocation no-auto-remediation decision | OPEN, undecided | When a merchant is revoked, `executePull()` hard-reverts silently forever. Decide: accept as-is, or build off-chain remediation. Flag to Hashlock as an audit design question either way. |
| T13 | Postgres autovacuum tuning | OPEN, not investigated | Carried across multiple sessions, no action taken yet |
| T14 | MerchantRegistry admin transfer to Safe (deferred to mainnet) | OPEN, deliberately deferred | **Do not confuse with SubscriptionVault's admin — that one is already the Safe, confirmed Aug 18.** This is a *different*, still-pending transfer, for a *different* contract. |
| T15 | `config.js` had a second, unused `wagmiConfig` export | ~~OPEN~~ **DONE** | Removed, deployed, commit `c79e32b` — kept here briefly as a closed example of the T6 pattern, remove from this list next session |
| T16 | `keeper.js` has no ERC-1271 pull support — every contract-wallet subscription is silently skipped, every cycle, forever | OPEN — Q3 mainnet scope decision made 2026-08-22: **ERC-1271 support IS in scope for mainnet launch** (Vasco: "everything ready for mainnet... fix as it is found," not deferred). No longer an open question — this is required work, not optional. Data question already resolved: confirmed via live Aug 21/22 boot logs (all 11 active subs skipped every cycle, 0 pulls) plus a direct DB query (Aug 22) filtered to the live vault: all 11 rows (ids 7–14,16–18) use owner `0xBE6a5cFF...E9e35` (`Subscriber` test wallet) and merchant either `0x4503E05F...2088F` (`Merchant`) or `0xF6CcD952...B9E5` (`merch test`) — confirmed leftover test data, not real subscribers, so no live subscriber is currently blocked. | Flagged narratively since July 24 (CLAUDE-CORE.md §26/29/30/35), added as a numbered item Aug 21, scoped into mainnet Aug 22. This is real engineering — signing per SV pull digest, ERC-1271 verification round-trip in `keeper.js` — not a quick patch. Needs its own estimate before it's sequenced against T1/T2/T4; do not treat as a same-session fix-as-found item. The Aug 10/11 `vault_address` query bug (querying by `id` alone pulls in unrelated legacy pre-v9 rows) reconfirmed live this session — always filter `subscriptions` queries by `vault_address` to the current vault. |
| T17 | Alchemy API key exposed in plaintext — pasted into this chat session inside a full RPC URL in a keeper boot log (Aug 21 2026) | **DONE** — 4th credential-exposure incident on this project (after the Postgres password rotation, §20, and two separate CDP key exposures, §41). Key rotated, Railway + `.env` updated (Aug 22). Closed with real evidence, not just the rotation claim: Alchemy dashboard screenshot (Aug 22, ~21:08) shows 24h Request Health at 0% invalid/0% throughput-limited, plus a live 5-minute request log — app "Authonce", `eth_getLogs`/`eth_chainId`/`eth_blockNumber`/`eth_call`, all HTTP 200, 0-2ms, zero errors. Relies on standard Alchemy behavior that resetting a key invalidates the old value immediately — if instead a second key was added without revoking the first, this traffic doesn't prove which key served it. Vasco has not contradicted the standard-reset assumption. | None — closed. Keep redacting the key portion of any RPC URL before pasting logs, going forward. |

---

*File created: 2026-08-20, from a full retrospective across this project's chat history. Update in place — don't let this drift back into narrative form.*
*Last updated: 2026-08-22 — mainnet scope directive from Vasco: "everything ready for mainnet, fix as it is found, don't let small items accumulate." Applies going forward to all OPEN items on this list, not just T16. Risk: some items (T16) are scoped engineering, not quick fixes — flag size before absorbing into "fix as found," don't let scope creep silently push the Q3 date.*
